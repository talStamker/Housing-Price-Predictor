# Housing Price Predictor - Frontend

## Setup
```bash
npm install
npm run dev
```

The app runs on http://localhost:8080 and connects to the backend at http://localhost:5000.

Make sure the backend is running first!
